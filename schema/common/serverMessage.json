{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ServerMessage",
  "type": "object",
  "properties": {
    "type": {
        "type": "string",
        "enum": ["uuid assignment","successful reconnect", "failed reconnect"]
    }
  },
  "required": ["type"],
  "additionalProperties": false
}